<!DOCTYPE html>
<html lang="en">
  <head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Purple Admin</title>
    <!-- plugins:css -->
    <link rel="stylesheet" href="../../assets/vendors/mdi/css/materialdesignicons.min.css">
    <link rel="stylesheet" href="../../assets/vendors/css/vendor.bundle.base.css">

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Prompt:wght@300;500&display=swap" rel="stylesheet">
    <!-- Layout styles -->
    <link rel="stylesheet" href="../../assets/css/style.css">
    <!-- End layout styles -->
    <link rel="shortcut icon" href="../../assets/images/favicon.ico" />
  </head>
  <body>
    <%- include('layout/headbarU')-%>
    <div class="container-scroller">
      <div class="container-fluid page-body-wrapper full-page-wrapper">
        <div class="content-wrapper d-flex align-items-center auth">
          <div class="row flex-grow">
            <div class="col-lg-8 mx-auto">
              <div class="col-12 grid-margin stretch-card">
                <div class="card">
                  <div class="card-body">
                    <% if (UserData.role === 'พนักงาน') { %>
                      <a href="/homeU">
                      <button type="button" class="btn btn-outline-primary" style="font-family: 'Prompt', sans-serif; font-size: 14px;">กลับ</button>
                      </a>
                    <% }  if (UserData.role === 'เจ้าของร้าน') { %>
                      <!-- Redirect to home for เจ้าของ -->
                      <a href="/home">
                      <button type="button" class="btn btn-outline-primary" style="font-family: 'Prompt', sans-serif; font-size: 14px;">กลับ</button> 
                    </a>  
                    <% } %>
                    <div class="text-center navbar-brand-wrapper d-flex align-items-center justify-content-center">
                      <i class="mdi mdi-border-color"></i>
                      <h1 style="font-family: 'Prompt', sans-serif; font-size: 18px;">แก้ไขโปรไฟล์ส่วนตัว</h1> 
                    </div>                               
             <!-- productU.ejs -->
             <form class="forms-sample" action="users/updateU" method="POST" enctype="multipart/form-data">
              <div class="form-group">
                <div class="image-container">
                  <img src="/images/users/<%= UserData.image %>" class="img-fluid circular-image" id="avatarImage">
                </div>
                <div class="form-group">
                  <label>File upload</label>
                  <input type="file" name="image" id="image" class="file-upload-default">
                  <div class="input-group col-xs-12">
                    <input type="text" class="form-control file-upload-info" disabled placeholder="Upload Image">
                    <span class="input-group-append">
                      <button class="file-upload-browse btn btn-gradient-primary" type="button">Upload</button>
                    </span>
                  </div>
                </div>
              </div>

  <style>
   .image-container {
                     display: flex;
                    justify-content: center;
                    align-items: center;
                    height: 200px;
                    }
                    .circular-image {
                      width: 150px;
                      height: 150px;
                      border-radius: 50%;
                      overflow: hidden;
                      object-fit: cover;
                    }
  </style>


<script src="https://code.jquery.com/jquery-3.6.4.min.js"></script>
<script>
  $(document).ready(function() {
    $('.file-upload-browse').on('click', function() {
      $('.file-upload-default').click();
    });

    $('.file-upload-default').on('change', function() {
      // Read and display the selected image
      const input = this;
      if (input.files && input.files[0]) {
        const reader = new FileReader();

        reader.onload = function (e) {
          $('#avatarImage').attr('src', e.target.result);
        };

        reader.readAsDataURL(input.files[0]);
      }

      // Update the file input text
      $('.form-control.file-upload-info').val($(this).val().replace(/C:\\fakepath\\/i, ''));
    });
  });
</script>

  <br>
  <div class="form-group">
    <!-- <label for="productId" hidden>Product ID</label> -->
    <input type="text" class="form-control form-control-lg" id="updateP_id" name="updateP_id" value="<%= UserData._id %>" 
    style="font-family: 'Prompt', sans-serif; font-size: 14px;" readonly hidden>
  </div>
  <!-- <div class="form-group">
      <label for="exampleInputName1">Username (ไม่สามารถแก้ไขได้)</label>
      <input type="text" class="form-control" id="exampleInputName1" value="<%= UserData.username %> ตำแหน่ง:<%= UserData.role %>" placeholder="Name" readonly>
  </div> -->
  <div class="form-group">
      <label for="exampleInputName1">ชื่อจริง</label>
      <input type="text" class="form-control" name="firstname" id="exampleInputName1" value="<%= UserData.firstname %>" placeholder="Name">
  </div>
  <div class="form-group">
      <label for="exampleInputName1">นามสกุล</label>
      <input type="text" class="form-control" name="lastname" id="exampleInputName1" value="<%= UserData.lastname %>" placeholder="Name">
  </div>
  <div class="form-group">
      <label for="exampleInputEmail3">Email address</label>
      <input type="email" class="form-control" name="email" id="exampleInputEmail3" value="<%= UserData.email %>" placeholder="Email">
  </div>
  <div class="form-group">
      <label for="exampleInputPassword4">phone</label>
      <input type="text" class="form-control" name="phone" id="exampleInputName1" value="<%= UserData.phone %>" placeholder="Name">
  </div>
  <div class="form-group">
      <label for="exampleInputCity1">City</label>
      <input type="text" class="form-control" name="address" id="exampleInputCity1" value="<%= UserData.address %>" placeholder="Location">
  </div>
  <button type="submit" class="btn btn-gradient-dark me-2">Submit</button>
  <a href="/homeU" class="hidden-link">
    <button type="button" class="btn btn-light">Cancel</button>
  </a>
</form>



<!-- Include Axios script for handling file selection -->
<script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
<script>
  // Function to trigger file input click
  function triggerFileInputClick() {
      document.getElementById('image').click();
  }

  // Function to handle file selection
  function handleFileSelect(event) {
      const input = event.target;
      if (input.files && input.files[0]) {
          const reader = new FileReader();

          reader.onload = function (e) {
              const avatarImage = document.getElementById('avatarImage');
              avatarImage.src = e.target.result;
          };

          reader.readAsDataURL(input.files[0]);
      }
  }
</script>

    <!-- container-scroller -->
  
    <!-- ... (unchanged) ... -->
    


                
              
    <!-- container-scroller -->
    <!-- plugins:js -->
    <script src="../../assets/vendors/js/vendor.bundle.base.js"></script>
    <!-- endinject -->
    <!-- Plugin js for this page -->
    <!-- End plugin js for this page -->
    <!-- inject:js -->
    <script src="../../assets/js/off-canvas.js"></script>
    <script src="../../assets/js/hoverable-collapse.js"></script>
    <script src="../../assets/js/misc.js"></script>
  </body>
</html>